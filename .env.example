# Backend the /api rewrite proxies to during dev / on the server:
API_PROXY_TARGET=http://localhost:3000

# Optional: make the browser call the backend directly instead of the rewrite.
# If set, the client fetches this origin (backend must allow CORS).
# NEXT_PUBLIC_API_BASE=https://mailmetric-api.vercel.app
