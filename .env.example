# Public backend origin used by browser API requests:
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Server-side fallback for relative /api requests when the public URL is empty:
API_PROXY_TARGET=http://localhost:4000
